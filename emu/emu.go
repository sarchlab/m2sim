// Package emu provides functional ARM64 emulation.
// It executes ARM64 instructions without timing information,
// used for correctness verification.
package emu

// Emulator executes ARM64 instructions functionally.
type Emulator struct {
	// TODO: Add register file, memory, PC
}
